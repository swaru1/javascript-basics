// Check if a number is an Armstrong number (sum of each digit to the power of number of digits equals the number).
// Example: 153 → 1³+5³+3³= 153

// let str = '548834';
// let num = Number(str);

// let sum = 0;
// let pwr = str.length;

// for( let i = 0; i < str.length ; i++) {
//     sum += Number(Math.pow(str[i], str.length));
//     console.log(sum);
// }

// if(sum == num) {
//     console.log('is Armstrong number')
// } else {
//     console.log('not arm no')
// }

//remove duplicates from an array using only loop not set

// let arr = [1, 2, 2, 5, 1, 3, 4, 5, 5];

// for ( let i = 0; i < arr.length; i++) {
//     for ( let j = i+1; j < arr.length; j++ ){
//         if( arr[i] == arr[j]) {
//             arr.splice(j, 1);
//             j--;
//         }
//     }
// }

// console.log(arr);

//Count how many times each character appears in a string using loops
str = 'aabaraakaadabarra'

for (let i = 0; i < str.length; i++) {
    for (let j = i+1 ; i < str.length; j++) {
        
    }
}

